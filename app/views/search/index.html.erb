<h1> Results</h1>

<% @results.each do |result| %>
  <%= image_tag(result[:small])%>
  <%=  button_to 'Save Image', images_path, params: { artist: result[:artist], portfolio: result[:portfolio], small: result[:small], full: result[:full], user_id: current_user.id}, method: :post%>
<% end %>